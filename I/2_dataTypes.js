const introduction =
    "As we saw at the variables JS, there's different kinds of data types. Strings, numbers, booleans, etc." +
    "Please check the code for a deeper illustration of the concept!";

const name = "John"; // this is a string datatype. a string is text, you can save to it even numbers, but JS will se text
const age = 47; // this is an int datatype. int are numbers.
const salute = "Hello, "; // this is also a string datatype
const boolean = true; // a boolean datatype is the one that can be only true or false.
const boolean1 = false;

console.log(salute, name, "Your age is: ", age, "and life is", boolean, "and not", boolean1)
                                                // console is a javascript class. log is a method of the console class
                                                // console.log means that you want to represent some info of that var.
                                                // methods and functions use () for initializing and adding to it some
                                                // parameters. like when you say "console.log(name);" you're giving
                                                // to the machine the variable "name" for it to be represented on the
                                                // console.

magicalAct(introduction);